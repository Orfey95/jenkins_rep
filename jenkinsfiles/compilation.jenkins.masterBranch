#!groovy
properties([disableConcurrentBuilds()])
pipeline {
    agent any  
    tools {
        jdk 'my-jdk'
        maven 'my-maven'
    }
    environment {
        version = "calculator-1.3-SNAPSHOT"
    }
    stages {            
	    
	    stage("packaging") {
            steps {
			sh 'mvn package'
		   } 				  				  
             }	    	    
        }	
   }
